function buildBodyWeightRecord(weight, unit, date) {
  // this object holds the user's body weight data

  record = {
    // number
    bodyWeightNumber: weight,

    // string
    measurementUnit: unit,

    // string
    dateCreated: date,
  };

  return bodyWeightObject;
}
