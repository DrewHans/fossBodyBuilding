<template>
  <div class="page" data-name="edit-workout">

    <div class="navbar">
      <div class="navbar-bg"></div>
      <div class="navbar-inner sliding">
        <div class="left">
          <a href="#" class="link back">
            <i class="icon icon-back"></i>
            <span class="if-not-md">Back</span>
          </a>
        </div>
        <div class="title">Edit Workout</div>
      </div>
    </div>

    <div class="page-content">
      <p>Hi, you made it to the Edit Workout page!</p>
    </div>
  </div>
</template>

<script>
  export default (props, { $, $on, $f7 }) => {
    // TODO: write function to load workoutInfo
    // const workoutId = props.id; // passed in from page url (see routes.js)
    // const workoutInfo = getWorkoutItem(workoutId);

    $on('pageInit', () => {

      // load event functions

      $('#saveWorkoutInfoButton').on('click', async () => {
        // collect food data
        // save food data to db
        // return to previous page
      });

    });

    return $render;
  }
</script>